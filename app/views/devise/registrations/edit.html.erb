<h1 id="page_title">회원정보 수정하기</h1>

<div class="skinny_wrapper wrapper_padding">

  <h2><%= resource_name.to_s.humanize %> 정보 수정하기</h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= devise_error_messages! %>

    <div class="field">
      <%= f.label :email, "이메일" %><br />
      <%= f.email_field :email, autofocus: true %>
    </div>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
    <% end %>

    <div class="field">
      <%= f.label :password, "비밀번호" %> <i>(수정을 원하지 않는다면, 빈칸으로 두세요.)</i><br />
      <%= f.password_field :password, autocomplete: "off" %>
    </div>

    <div class="field">
      <%= f.label :password_confirmation, "비밀번호 확인" %><br />
      <%= f.password_field :password_confirmation, autocomplete: "off" %>
    </div>

    <div class="field">
      <%= f.label :current_password, "현재 비밀번호" %> <i>(비밀번호를 수정하기 위한 현재 비밀번호 확인이 필요합니다.)</i><br />
      <%= f.password_field :current_password, autocomplete: "off" %>
    </div>

    <div class="actions">
      <%= f.submit "회원정보 수정하기" %>
    </div>
  <% end %>

  <hr>

  <h3>회원 탈퇴하기</h3>

  <p>탈퇴하시려면 아래 버튼을 클릭해주세요.</p>
  <%= button_to "탈퇴하기", registration_path(resource_name), data: { confirm: "정말로 탈퇴하시겠습니까?" }, method: :delete %>
  <hr>
  <%= link_to "메인 페이지로 돌아가기", :back %>

</div>